# Import the 'bot' module
import bot

# Check if the script is being run as the main program
if __name__ == '__main__':
    # If so, call the 'run_discord_bot' function from the 'bot' module to start the Discord bot
    bot.run_discord_bot()
